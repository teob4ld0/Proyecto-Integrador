# URL base del servidor backend (sin /api al final).
# En desarrollo NO definir esta variable — Vite usa su proxy interno hacia /api.
# En producción o con Docker definir la URL completa del servidor:
# VITE_API_URL=http://localhost:8080